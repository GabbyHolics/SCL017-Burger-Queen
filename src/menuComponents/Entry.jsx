import React from 'react'

// const Entry = ({data, listOrder, setListOrder, fullData}) => {
//   console.log(setListOrder)
    
// //   const {id, type, price} = data
// //     const addData= (id) =>   {
// //         const newData = fullData.filter((item) => item.id === id)
// //         setListOrder([
// //             ...listOrder,...newData,
// //         ])
// //         console.log(listOrder.length)
// //     }
//     return (

//         <div>
//             <li  className="btn btn-danger m-1" key={id} onTouchStart={(() => addData(id))} >{type} $ {price}</li>
// {/* 
//             {
//                 dataMenu.map((item)=>(
                    
//                     <li  className="btn btn-danger m-1"  onClick={() => addEntry(item.id)}  key={item.id}> {item.type} $ {item.price} </li>

//                 ))
//             } */}
//         </div>
//     )
// }

// export default Entry
